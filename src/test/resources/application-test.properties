# Test configuration for ExcelService tests
# Provide a dummy OpenAI API key for testing (not used in ExcelService tests)
spring.ai.openai.api-key=test-key-not-used
spring.ai.openai.chat.api-key=test-key-not-used
